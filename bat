import bayesian 
import utility 
import vectorrelated 
import knn 
import globalvariable
reload(bayesian);reload(utility); reload(vectorrelated); reload(knn); reload(globalvariable);knn.evaluate("./testset/b.txt")

